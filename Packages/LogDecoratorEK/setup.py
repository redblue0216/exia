from setuptools import setup,find_packages

setup(
        name = 'LogDecoratorEK',
        version = '0.1',
        packages = find_packages(),
        author = 'shihua',
        description = 'LogDecorator module',
        install_requires = ['CMRESHandler']
)