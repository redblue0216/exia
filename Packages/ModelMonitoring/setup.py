from setuptools import setup,find_packages

setup(
        name = 'ModelMonitoring',
        version = '0.1',
        packages = find_packages(),
        author = 'shihua',
        description = 'ModelMotitoring module',
        install_requires = ['transitions']
)